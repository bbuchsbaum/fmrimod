# contrast.contrast_spec.contrast { #fmrimod.contrast.contrast_spec.contrast }

```python
contrast(form, name, where=None)
```

Define a linear contrast using a formula expression.

Accepts a :class:`Formula`, a string predicate, a dict, or a callable for
``form`` and ``where``.