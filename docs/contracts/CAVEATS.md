# Parity Caveats Index

This index lists every active parity caveat that appears in generated reports.
A caveat is not a tolerance escape hatch: it must name the affected quantity,
the current reason for divergence, an owning work item, and a concrete exit
criterion.

| Caveat ID | First appearance | Affected tiers | Owner | Exit criterion |
| --- | --- | --- | --- | --- |
| `localizer-tstat-variance-outliers` | `benchmarks/parity/tier_a_localizer_fixed_effects/` | Tier A localizer fixed effects | `bd-01KRGRF4X588F56QT21CPBB7NJ` | Resolve the remaining near-zero-dispersion sparse-mask voxels where Nilearn `run_glm` and fmrimod's X/Y-aware OLS path produce different variance-derived t statistics, then restore the normal allclose gate. |
| `fitlins-ar1-coefficient-binning` | `benchmarks/parity/tier_b_fitlins_bids/` | Tier B FitLins CLI derivative parity | `bd-01KRGRF4X588F56QT21CPBB7NJ` | Narrow or remove the AR/statistic caveat once t and variance maps meet the standard gate without undocumented tolerance bypasses. |
| `spm-canonical-r-parity-divergence` | Tier A R parity suite (early 2026) | `tests/design/test_r_parity.py` SPMG-realised value tests | bbuchsbaum/fmrihrf SPM-canonical alignment ticket | fmrimod's default `basis="spm"` was aligned to the SPM/Nilearn standard parameterization (`delay=6, undershoot=16, dispersion=1, u_dispersion=1, ratio=0.167`); the R `fmrireg` package still uses the legacy `(p1=5, p2=15, a1=0.0833)` form. The R-side fixtures pin the legacy shape, so the SPMG-realised R parity tests are marked `xfail(strict=True)`. Exit: bbuchsbaum/fmrihrf adopts the SPM parameterization, fmrireg follows, R fixtures are regenerated, xfails removed. The legacy fmrimod path remains available via `basis="spm_legacy"`. |
| `spm-realized-column-sampling-convention` | Tier A SPM parity probes (early 2026) | Pattern A SPM-canonical workflows when frame_times conventions diverge | informational — configurable via API | fmrimod's default sampling grid uses the BOLD-midpoint convention (`t = TR/2 + k·TR`); Nilearn / SPM-MAT / FitLins use frame-start (`t = k·TR`). The midpoint default reflects "the value at sample `k` is the BOLD signal at the middle of the `k`-th TR window"; frame-start reflects "sample `k` is the model prediction at the moment acquisition `k` started." Both are valid; the choice is configurable per dataset via `fmri_dataset(..., slice_timing_offset=0.0)` / `matrix_dataset(..., slice_timing_offset=0.0)`. With matched grids and the default TR-relative precision (`min(TR)/16`), fmrimod's realised columns match Nilearn at correlation > 0.999 / max abs diff < 1e-3. Closing-the-caveat criterion: workflows that need Nilearn parity pass `slice_timing_offset=0.0`; no further code fix planned. |
| `fir-basis-convention-divergence` | `benchmarks/parity/tier_a_fir_basis/` (early 2026) | Pattern A FIR-basis workflows | informational — convention difference, not bug | fmrimod's FIR basis differs from Nilearn's in two conventions: (1) **delay indexing** — fmrimod's `basis_ix=1` places the impulse at frame_time = onset, Nilearn's `delay_1` places it at frame_time = onset + 1·TR (off by one bin); (2) **amplitude normalization** — fmrimod's FIR impulse has amplitude 1.0 (the natural physical interpretation), Nilearn's has amplitude `1/oversampling = 0.02` (an integration normalization keeping the realised regressor on the same numerical scale as a convolved boxcar). Both are valid in the field. Pattern A column-by-column parity is not achievable without re-aligning conventions; Pattern B (fmrimod realises the design, both engines solve on the same X) closes the loop and is the structure of `tier_a_fir_basis/workflow.py`. Exit: if either convention should be configurable, file a work-request analogous to the SPM canonical alignment (S2 because it changes a stable basis function). |
| `parametric-modulator-r-parity-divergence` | Tier A R parity suite (early 2026) | `tests/design/test_r_parity.py::TestParametricModulation` | bbuchsbaum/fmridesign modulator-centering alignment ticket | fmrimod's typed-spec `hrf(..., modulators=(...))` was aligned to the modern-correct workflow in early 2026: `center_modulators=True` by default, applied at the input-variable level (pre-convolution) on raw values. This matches `EventVariable(center=True)`, `Scale(center=True)`, and orthogonal `Poly` defaults across the codebase. R `fmridesign` historically did not center modulators, so its parametric-modulation fixtures pin the uncentered shape. The R parity test is marked `xfail(strict=True)` until R-side fixtures regenerate against an aligned fmridesign. The legacy uncentered path remains available via `hrf(..., center_modulators=False)`. |

## Maintenance Rule

Generated reports may introduce a new caveat only after this file is updated in
the same change. The test suite checks that every structured `caveat_id` in the
checked-in parity reports appears in this index.
Rows in this index must also have live owning work items unless they explicitly
name a closed replacement owner in the exit criterion.
